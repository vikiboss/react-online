/**
 * @deprecated This file is kept for backward compatibility only.
 * Please import from '@/stores' instead:
 * - import { configStore, filesStore, editorStore } from '@/stores'
 */

// Re-export everything from new stores structure
export * from './stores'
